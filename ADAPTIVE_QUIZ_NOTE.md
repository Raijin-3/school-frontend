Adaptive AI Quiz vs. Open Quiz (basic)
---------------------------------------

The adaptive AI quiz lives outside the Open Quiz (basic) runner flow; it has its own
state, resume controls, and summary mechanics, so we intentionally keep them distinct and
don’t merge their behaviors in the subject learning interface.
